# Project basic information configuration, required.
[project]
name = "datus-agent"
version = "0.3.9"
description = "AI-powered SQL Agent for data engineering (Compiled Version)"
readme = "README.md"
requires-python = ">=3.12"
license = "Apache-2.0"
license-files = ["LICENSE*"]
authors = [
    { name = "Datus Team", email = "harrison.zhao@datus.ai" }
]
maintainers = [
    { name = "Datus Team", email = "harrison.zhao@datus.ai" }
]
keywords = ["sql", "ai", "agent", "database", "nlp", "natural-language"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Topic :: Database",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
]
dependencies = [
    "datus-db-core>=0.1.5",
    "datus-storage-base>=0.1.5,<0.2.0",
    "python-dotenv==1.0.0",
    "pandas==2.1.4",
    "sqlalchemy==2.0.23",
    "sqlglot>=26.12.0,<31",
    "pyyaml==6.0.1",
    "packaging>=21.0",
    "structlog>=23.1.0",
    # Cap below 2.45.0: openai 2.45.0 made ``InputTokensDetails.cache_write_tokens``
    # a required field, which openai-agents 0.7.0 does not populate when it default-
    # constructs ``Usage()`` (agents/usage.py), raising a pydantic "Field required"
    # error on every model turn. Lift the cap only when bumping openai-agents.
    "openai>=2.8.0,<2.45.0",
    "httpx[socks]==0.27.2",
    "tantivy>=0.22.2",
    "aiohttp>=3.11.16",
    "xlsxwriter>=3.2.2",
    # Spreadsheet ingestion (``load_file_as_table``). DuckDB's excel extension
    # reads sheet *contents* but cannot enumerate sheet names or report the used
    # range, both of which are needed to build a correct VIEW; openpyxl supplies
    # them. xlrd covers legacy .xls, which DuckDB does not support at all.
    "openpyxl>=3.1.5",
    "xlrd>=2.0.1",
    "openai-agents[litellm]==0.7.0",
    "openinference-instrumentation-openai-agents==1.6.2",
    "opentelemetry-api>=1.20.0",
    "opentelemetry-sdk>=1.20.0",
    "opentelemetry-exporter-otlp>=1.20.0",
    # Upper bound tracks the ``httpx[socks]==0.27.2`` pin above: litellm 1.83.1+ requires
    # ``httpx==0.28.1`` / ``httpx>=0.28``, so nothing above 1.83.0 can ever be selected.
    # Without the bound, pip backtracks from the newest release through ~90 versions and
    # source-builds the 1.92-1.95 sdists, which ship no macOS or Windows wheels — minutes
    # stuck on "Preparing metadata". Lift this bound only together with the httpx pin.
    "litellm>=1.67.4.post1,<1.83.1",
    "pydantic>=2.11.7,<3.0",
    "lancedb==0.34.0",
    # This library is required by lancedb
    "pyarrow<19.0.0",
    "rich==14.0.0",
    "prompt_toolkit>=3.0.51",
    "pygments>=2.18.0",
    "textual[syntax]==5.1.1",
    "anthropic==0.51.0",
    "duckdb-engine>=0.17.0",
    "duckdb==1.5.2",
    # MCP (Model Context Protocol) support
    "mcp>=1.11.0",
    "anyio>=4.9.0",
    "json-repair>=0.47.6",
    "fastapi>=0.104.0,<1.0",
    "uvicorn>=0.24.0",
    "pyperclip==1.9.0",
    "wcmatch>=10.0",
    "fastembed==0.4.2",
    # Optional: install torch manually to enable reranker models.
    "charset-normalizer>=3.4.2",
    "defusedxml>=0.7.1",
    # Platform documentation tools
    "PyGithub>=2.1.0",
    "beautifulsoup4>=4.12.0",
    "lxml>=5.0.0",
    "markdown-it-py>=3.0.0",
    "tabulate>=0.9.0",
    "datus-semantic-core>=0.2.3",
    # BI platform adapters (core models & interfaces)
    "datus-bi-core>=0.1.2",
    # Workflow scheduler adapters (core framework)
    "datus-scheduler-core>=0.1.1",
    # Plugin manifest: config-schema validation and system-prompt templates
    "jsonschema>=4.17,<5",
    "jinja2>=3.1,<4",
]

[project.urls]
Homepage = "https://datus.ai/"
Documentation = "https://docs.datus.ai"
Repository = "https://github.com/datus-ai/datus-agent"
"Bug Tracker" = "https://github.com/datus-ai/datus-agent/issues"

[project.scripts]
datus-agent = "datus.entrypoints:agent_main"
datus-cli = "datus.entrypoints:cli_main"
datus = "datus.entrypoints:cli_main"
datus-api = "datus.entrypoints:api_main"
datus-mcp = "datus.entrypoints:mcp_main"
datus-gateway = "datus.entrypoints:gateway_main"

[project.entry-points."datus.semantic_adapters"]
cube = "datus_semantic_cube:register"

[build-system]
requires = ["setuptools>=80.9.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["datus*"]
exclude = ["benchmark*", "docs*"]

[tool.setuptools.package-data]
"*" = ["*.yml", "*.yaml", "*.json", "*.md", "*.txt", "*.j2"]
"datus" = [
    "*.yml", "*.yaml", "*.json", "*.md", "*.txt",
    "sample_data/*.duckdb",
    "sample_data/california_schools/*.csv",
    "sample_data/california_schools/*.sqlite",
    "sample_data/california_schools/reference_sql/*.sql",
    "sample_data/california_schools/reference_template/*",
    "sample_data/superset/*",
]
"datus.cli.web" = ["templates/*.html"]
"datus.agent.node.visual_artifact" = ["templates/*.html"]
"datus.prompts" = ["*.txt", "*.md", "*.j2"]
"datus.prompts.prompt_templates" = ["*.j2"]

[tool.setuptools.data-files]
"datus/conf" = ["conf/*.yml", "conf/*.yml.*"]

# Ruff: replaces black + isort + flake8
[tool.ruff]
line-length = 120
target-version = "py312"
extend-exclude = ["mcp/"]

[tool.ruff.format]
# Compatible with black defaults

[tool.ruff.lint]
select = [
    "E", # pycodestyle errors
    "W", # pycodestyle warnings
    "F", # pyflakes
    "B", # flake8-bugbear
    "I", # isort
    "C90", # mccabe complexity
    "T20", # flake8-print (enforce "no print()" guardrail)
]
ignore = [
    "E203", # whitespace before ':'
    "E266", # too many leading '#' for block comment
    "E501", # line too long (handled by formatter)
    # TODO: enable these rules incrementally in follow-up PRs
    "C901", # function too complex (218 existing violations)
    "B904", # raise without from inside except (22 existing violations)
    "B905", # zip without explicit strict (21 existing violations)
    "B027", # empty method without abstract decorator
    "B007", # unused loop control variable
    "B019", # cached instance method
    "B024", # abstract base class without abstract method
    "T201", # print found (63 existing violations, clean up incrementally)
]

[tool.ruff.lint.mccabe]
max-complexity = 10

[tool.ruff.lint.isort]
known-first-party = ["datus"]
known-third-party = ["mcp"]

[tool.pytest.ini_options]
pythonpath = "."
testpaths = ["tests"]
# markers are defined in pytest.ini to avoid duplication

[dependency-groups]
# uv sync --dev
dev = [
    "pytest==8.0.2",
    "pytest-asyncio>=0.23.8",
    "jsonschema>=4.25.1,<5",
    # Keep consistent with the workflow version of github
    "ruff==0.15.6",
    "mypy==1.8.0",
    "tqdm>=4.27.0",
    "pytest-timeout>=2.3.1",
    "pytest-xdist>=3.7.0",
    "pytest-rerunfailures>=14.0",
    "pre-commit>=4.2.0",
    "setuptools>=80.9.0",
    "pip>=25.1.1",
    "build>=1.2.0",
    "twine>=6.1.0",
    "langsmith>=0.7.0",
    "langsmith-fetch>=0.1.0",
    "langfuse>=3.0.0",
    "pytest-cov>=7.0.0",
    "chardet>=3.0.2,<6",
    "diff-cover>=10.2.0",
    # Writes legacy .xls fixtures. Only a *writer* is needed for tests; the
    # runtime reads .xls through xlrd, which is a real dependency.
    "xlwt>=1.3.0",
]
ci = [
    "datus-storage-postgresql>=0.1.5",
    # dosi-engine publishes manylinux wheels only, so the marker keeps a macOS
    # checkout resolvable; that platform builds the engine from source instead.
    "datus-semantic-dosi>=0.1.6 ; platform_machine == 'x86_64' and sys_platform == 'linux'",
]

[tool.coverage.run]
source = ["datus"]
omit = [
    "datus/prompts/prompt_templates/*",
    "datus/cli/screen/*",
    "datus/cli/sub_agent_wizard.py",
    "datus/cli/profile_picker_app.py",
]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "if __name__ == .__main__.",
    "raise NotImplementedError",
    "except ImportError",
    "if TYPE_CHECKING:",
]
show_missing = true
precision = 2
